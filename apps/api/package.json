{
  "name": "@riddles/api",
  "version": "0.1.0",
  "private": true,
  "description": "Delivery and ingestion API: serves venues, tracks, and bundles; ingests session event batches idempotently; derives leaderboard entries with the shared game rules.",
  "type": "module",
  "scripts": {
    "start": "node src/main.ts",
    "seed": "node src/seed.ts",
    "typecheck": "tsc -p tsconfig.json",
    "test": "node --test \"src/**/*.test.ts\""
  },
  "dependencies": {
    "@electric-sql/pglite": "^0.2.17",
    "@fastify/cookie": "^11.1.2",
    "@fastify/static": "^8.3.0",
    "@riddles/bundle-builder": "*",
    "@riddles/bundle-schema": "*",
    "@riddles/game-core": "*",
    "drizzle-orm": "^0.44.0",
    "fastify": "^5.2.0",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/pg": "^8.11.0",
    "typescript": "^5.9.0"
  }
}
